apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: public-allow-ingress
  namespace: default
spec:
  podSelector:
    matchLabels:
      app: public
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: ingress-nginx
      ports:
        - protocol: TCP
          port: 8080
